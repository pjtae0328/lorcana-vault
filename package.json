{
  "name": "lorcana-catalog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-data": "node scripts/fetch-data.mjs",
    "update": "node scripts/fetch-data.mjs && vite build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
